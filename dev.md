
Nouvelle version :
ne pas oublier de modifier le fichier `custom_components/philips_dline_sicp/manifest.json

```bash
git add .
git commit -m "commit message"
git tag v1.2.3
pit push origin v1.2.3
```